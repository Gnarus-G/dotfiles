#!/bin/sh

export SCRIPTPATH="$(
  cd "$(dirname "$0")"
  pwd -P
)"

leftwm-command "UnloadTheme"

eww -c $SCRIPTPATH/.config/eww kill
sleep 1

Kill eww
pkill stalonetray
pkill picom
pkill dunst

echo "✅ Theme shutdown complete"
